/** Canonical origin for absolute URLs — metadataBase, the sitemap, robots.txt. */
export const SITE_URL = (process.env.NEXT_PUBLIC_SITE_URL || "https://chana-bergman.co.il").replace(/\/+$/, "");

export const SITE_NAME = "חנה ברגמן";
